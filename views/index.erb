<% if session[:message] %>
  <p><%= session.delete(:message) %></p>
<% end %>

<ul>
  <% @files.each do |file| %>
    <li><a href="<%= file %>"><%= file %></a> | <a href="<%= file %>/edit">edit</li>
  <% end %>
</ul>
